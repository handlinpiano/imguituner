#pragma once

namespace gui {

// Renders the Icon Browser window. The 'open' flag controls visibility and can be toggled by the window close button.
void render_icon_browser_window(bool& open);

} // namespace gui


